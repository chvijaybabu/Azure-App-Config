{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "configStoreName": {
            "value": "#{Azure.AppConfiguration.Name}"
        },
		"location": {
            "value": "#{Azure.AppConfiguration.Location}"
        },
		"sku": {
            "value": "#{Azure.AppConfiguration.Sku}"
        }	
    }
}